aws:
  s3:
    endpoint-url: ${AWS_ENDPOINT_URL:http://localhost:4566}
    public-endpoint-url: ${AWS_PUBLIC_ENDPOINT_URL:http://localhost:4566}
  dynamodb:
    endpoint-url: ${AWS_ENDPOINT_URL:http://localhost:4566}

dms:
  security:
    enabled: false
    require-user-id: true

logging:
  level:
    com.lostres.ms_documental_dms: DEBUG
